# 部署文档 - 生产环境上线指南

## 一、服务器环境要求

- 操作系统：Linux（Ubuntu 20.04+ / CentOS 7+）
- Python：3.11+
- Node.js：18+
- Web服务器：Nginx
- 应用服务器：Gunicorn
- Chromium（用于 SSG 预渲染）

---

## 二、部署流程

### 1. 后端部署（Django + Gunicorn + Nginx）

```bash
cd backend
pip install -r requirements.txt
pip install gunicorn
python manage.py migrate
python manage.py collectstatic --noinput
python manage.py shell < api/init_data.py
```

启动 Gunicorn：
```bash
gunicorn config.wsgi:application -b 127.0.0.1:8000 --workers 4 --daemon
```

### 1.1 后端环境变量（生产必配）

项目已支持以下环境变量，覆盖安全与生产配置（默认均为开发态，上线前务必设置）：

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `DJANGO_SECRET_KEY` | 内置占位串 | 生产请设为高强度随机串 |
| `DEBUG` | `True` | 生产设为 `False` |
| `ALLOWED_HOSTS` | `*` | 生产设为实际域名，逗号分隔（如 `www.xxx.cn,xxx.cn`） |
| `SERVE_SPA` | `False` | 同域托管前端时设为 `True`（Django 直接服务 `dist/`） |
| `SECURE_SSL_REDIRECT` | `False` | 生产设为 `True`（强制 HTTPS） |
| `SECURE_COOKIES` | `False` | 生产设为 `True`（Cookie 仅 HTTPS 传输） |
| `HSTS_SECONDS` | `0` | 生产建议 `31536000`（开启 HSTS，含子域与 preload） |

> 媒体文件（图片/文档/视频）由 Django 在 `DEBUG` 或 `SERVE_SPA` 模式下自动服务；生产若由 Nginx 托管，需在 Nginx 中配置 `/media/` 指向 `backend/media/`。

### 2. 前端构建 + SSG 预渲染

```bash
cd frontend
pnpm install
pnpm run build:prerender    # 构建 + 预渲染所有路由
```

构建产物位于 `frontend/dist/`，包含：
- 所有路由的预渲染 HTML（含真实正文 + JSON-LD）
- 哈希后的 JS/CSS 资源
- `robots.txt`、`sitemap.xml`、`llms.txt`

### 3. Nginx 配置（同域部署，推荐）

```nginx
server {
    listen 80;
    server_name laborsaving-arm.cn www.laborsaving-arm.cn;

    # 前端预渲染静态文件
    root /path/to/frontend/dist;
    index index.html;

    # SEO 静态文件
    location = /robots.txt { }
    location = /sitemap.xml { }
    location = /llms.txt { }

    # API 代理到 Django
    location /api/ {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # Django Admin
    location /admin/ {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # Django 静态文件
    location /static/ {
        alias /path/to/backend/staticfiles/;
    }

    # 前端 SPA fallback（预渲染的 HTML 优先）
    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

---

## 三、HTTPS 配置（推荐）

使用 Let's Encrypt 免费证书：
```bash
certbot --nginx -d laborsaving-arm.cn -d www.laborsaving-arm.cn
```

---

## 四、SEO 上线检查清单

- [ ] 后台配置所有站长验证代码（百度/360/搜狗/Google）
- [ ] 配置百度统计 ID、GTM ID（上线前填入真实值）
- [ ] 提交 sitemap.xml 至百度/360/搜狗/Google 站长平台
- [ ] 验证 llms.txt、robots.txt 可访问
- [ ] 验证各页面 TDK 不为空且无重复
- [ ] 验证预渲染 HTML 含真实内容（curl 检查）
- [ ] 使用百度统计实时访客验证代码生效
- [ ] 移动端适配测试（Google Mobile-Friendly Test / 百度移动适配检测）

---

## 五、运维监控

- 数据库定期备份：`cp backend/db.sqlite3 db_backup_$(date +%Y%m%d).sqlite3`
- 日志查看：`tail -f /var/log/nginx/access.log`
- 进程监控：Supervisor 守护 Gunicorn 进程
- 统计分析：登录百度统计后台查看流量数据

---

## 六、数据库管理（Navicat Premium Lite）

1. 打开 Navicat Premium Lite
2. 连接类型：SQLite
3. 选择数据库文件：`backend/db.sqlite3`
4. 连接后即可查看和编辑表数据

详细配置步骤见 `docs/Navicat配置教程.md`
