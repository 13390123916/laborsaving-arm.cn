# 本次对话执行规划（基于真实仓库 `laborsaving-arm.cn`）

> 本规划替代前一份"仅依项目规范推断"的版本，改为**基于真实代码盘点**。

---

## 0. 资料来源与重要说明

- **《执行跟踪节点文档.md》未能读取**：netdrive 下载接口对所有文件（含该文档）返回 COS `InvalidAccessKeyId`——根因是 netdrive 签名服务的系统时钟比真实时间**超前约 12 小时**，签名时效窗口整体落在未来，被 COS 拒绝；RAG 检索也因该文档刚创建（尚未建索引）无命中。GitHub 各仓库中均无此文件。
- **改用项目真实资产**：你提供的 GitHub 令牌让我克隆到了完整项目仓库 `13390123916/laborsaving-arm.cn`（含 Django 后端 + Vue 前端 + 全套 docs）。以下规划基于**真实代码盘点**，比规范推断更准。
- **⚠️ 文档与代码不一致**：仓库 `README.md` 描述的是"目标态 v1.1.0"（含构建期预渲染、10 条路由、Products/Franchise 页、JSON-LD、GTM 等），但**实际提交的代码是 v1.0.0 简化态**（6 条路由、无预渲染、产品硬编码）。规划以**实际代码**为准，README 视为待对齐的目标文档。

---

## 1. 当前真实状态盘点

### ✅ 已完成（可用）
| 模块 | 内容 |
|------|------|
| 后端模型 | `User` / `SiteConfig` / `Article` / `Faq` / `Contact` 五表，含扩展字段 |
| 后端接口 | `site-config/info/`、`articles/`(列表/详情/分类)、`faqs/`、`contacts/`(提交/统计) ViewSet + 序列化器 + Admin 后台 |
| SEO 路由 | 后端运行时生成 `llms.txt` / `robots.txt` / `sitemap.xml` / `schema/organization.json` |
| 种子数据 | 站点配置 + **15 条 FAQ**（达标）+ 3 篇资讯 |
| 前端骨架 | 6 路由 SPA、移动端响应式、联系表单 + 转化埋点（`_hmt` / `dataLayer`） |
| SEO 插件 | `plugins/seo.js` 已在 `main.js` 注册：启动时拉取配置并注入**统计脚本**与**四家站长验证 meta** |
| 验证占位 | `index.html` 已预留 百度/360/搜狗/Google 验证 meta 标签 |
| 文档 | API 接口文档 / Navicat 配置教程 / SEO 配置说明 / 部署文档 齐备 |

### 🔴 关键缺口（阻塞或不达标）
| 编号 | 缺口 | 违反的项目目标 | 严重度 |
|------|------|------|------|
| **G1** | `POST /contacts/` 缺 `@csrf_exempt`，DRF 默认 `SessionAuthentication` 会做 CSRF 校验 → 浏览器提交被 **403 拦截** | 目标4 转化闭环 | 🔴 阻断 |
| **G2** | 各页面**未注入独立 TDK**（始终为默认 `<title>`，`router.meta` 未生效） | 目标1 TDK 标准化（硬指标） | 🔴 缺失 |
| **G3** | 页面**未嵌入 JSON-LD 结构化数据**（FAQPage / Article / Organization），仅后端有独立 `/schema` 端点 | 目标1 结构化数据（硬指标） | 🔴 缺失 |
| **G4** | 360 自动收录脚本用了 `jspassport.360.cn/360/loader.js`，规范明文要求 `qhres2.com/sozz` | 目标1 360 收录 | 🟠 不符规范 |
| **G5** | 产品区为前端硬编码，无后台 `Product` 模型/接口（"所有页面数据后台可配置"未覆盖） | 目标2 闭环 | 🟠 功能缺口 |
| **G6** | 纯 SPA 无预渲染，首屏空壳，搜索引擎/AI 抓取弱（README 的 SSG 未落地） | 目标1 / 目标3 | 🟠 架构级 |
| **G7** | `llms.txt` 等 SEO 路由由 Django 提供，需确认生产环境前端托管方式（静态拷贝 or 反向代理） | 目标1 | 🟡 部署配置 |
| **G8** | `README.md` 与实际代码不一致（过时），易误导后续维护 | 规范性 | 🟡 文档 |

---

## 2. 本次对话优先级执行路线图

按项目官方优先级（**收录优化 > 功能闭环 > 体验 > 转化 > 背书 > 内容**）排列。

### 阶段 0（P0 · 必须先做，解锁闭环）✅ 本轮已执行
- **N1 修复 CSRF（G1）** ✅：`backend/api/views.py` 的 `ContactViewSet` 加 `@method_decorator(csrf_exempt, name='create')` → 表单提交由 403 变为 201。
- **N2 后端本地跑通** ✅：venv(Python3.11) + Django5.2/DRF3.17/cors4.9，`migrate` + `init_data`（站点配置 + 15 FAQ + 3 资讯），启动并冒烟测试 9 项接口全部通过（见第 5 节证据）。
- **附：400 内容不丢失（用户特别要求）** ✅：`Contact.vue` 出错时**不清空**已填内容，并把后端字段级错误（`data.{phone,message,...}`）拼成可读中文提示直接展示；仅成功才清空。

### 阶段 1（P1 · 收录优化硬指标，项目最高优先级）
- **N3 每页 TDK 注入（G2）**：`router.afterEach` 读取 `meta.title/keywords/description` → 调 `$seo.setTDK`；动态页（资讯详情）在视图内覆盖。
- **N4 结构化数据注入（G3）**：新增 `useJsonLd` 组合式，在 `Home`(Organization) / `Faq`(FAQPage) / `NewsDetail`(Article) 注入 JSON-LD，与后端 `/schema` 对齐。
- **N5 360 收录地址对齐（G4）**：改为规范的 `qhres2.com/sozz` 脚本。
- **N6 SEO 路由生产可达（G7）**：明确 Django 托管 `llms.txt`/`robots.txt`/`sitemap.xml` 或前端 `public/` 静态拷贝方案，确保部署后 AI 爬虫可取。

### 阶段 2（P2 · 闭环补全）
- **N7 产品后台化（G5）**：新增 `Product` 模型 + 接口 + Admin，`Home` 产品区改为接口渲染。
- **N8 资讯充实**：示例资讯由 3 篇增至 6–8 篇，覆盖多分类。

### 阶段 3（P3 · 进阶 / 多轮）
- **N9 预渲染 SSG（G6）**：引入 puppeteer 构建期快照，解决 SPA 空壳死穴。
- **N10 真实 Token 填入**：百度统计 / 360 / 搜狗 / Google 验证 + 统计 ID。
- **N11 README 与代码对齐（G8）**：消除过时文档。

---

## 3. 建议本轮落地范围

推荐本轮完成 **N1–N6（P0 + P1）** —— 这是"网站能跑、能被收录、转化可用"的最小必要集，且全部可在本环境用真实代码验证落地。N7–N11 留待下一轮。

> 说明：N1–N6 只改动本地克隆副本，**不会自动推送**到你的 GitHub 仓库；验证通过后由你决定是否提交。

---

## 5. 阶段 0 执行记录（本轮已完成）

**N1 · CSRF 修复** — `backend/api/views.py`
```python
from django.utils.decorators import method_decorator
from django.views.decorators.csrf import csrf_exempt

@method_decorator(csrf_exempt, name='create')
class ContactViewSet(viewsets.ViewSet):
    ...
```

**N2 · 后端本地跑通 + 冒烟测试**（venv: Python 3.11 / Django 5.2.16 / DRF 3.17.1 / cors 4.9.0）

| # | 接口 | 方法 | 结果 |
|---|------|------|------|
| 1 | `/api/site-config/info/` | GET | 200 · 企业名/成立年正常 |
| 2 | `/api/articles/` | GET | 200 · 3 篇资讯 |
| 3 | `/api/faqs/` | GET | 200 · 15 条 FAQ |
| 4 | `/api/contacts/` | POST | **201**（修复前 403）· 写入成功 |
| 5 | `/api/contacts/stats/` | GET | 200 · {total:1,handled:0,pending:1} |
| 6 | `/llms.txt` | GET | 200 |
| 7 | `/sitemap.xml` | GET | 200 |
| 8 | `/robots.txt` | GET | 200 |
| 9 | `/schema/organization.json` | GET | 200 |

**400 校验（内容不丢失验证）**
- 手机号过短 → `400 {"code":400,"data":{"phone":["请输入有效的联系电话"]}}`
- 缺必填 → `400 {"code":400,"data":{"phone":[...],"message":[...]}}`
- 前端 `Contact.vue`：失败时**保留**已填内容，并展示上述字段级中文错误；仅成功才清空。

**前端构建验证**：`pnpm install` + `pnpm build` 通过（94 模块，dist 生成），`Contact.vue` 改动编译无误。

**本轮改动文件**
- `backend/api/views.py`（CSRF 豁免）
- `frontend/src/views/Contact.vue`（400 错误展示 + 内容保留）
- `docs/本次对话执行规划.md`（本规划，随仓库提交）

---

## 4. 需你补充 / 确认

1. **各站长验证 Token 与统计 ID**（百度 `baidu_tongji`、360、搜狗、Google `gtm_id` / Ads）——用于 N10 真实生效（N1–N6 可先用占位跑通）。
2. **是否授权我把修复提交/推送到 `laborsaving-arm.cn`**？（目前只在本地克隆修改，未推送；令牌已从仓库地址中清除，已本地安全保存。）
3. 若《执行跟踪节点文档》有特定节点顺序或必做项，请**粘贴正文**，我据此把优先级校准到文档既有条目。
